# Shared

Código transversal usado por múltiplas camadas. Mantenha pequeno.

## Subpastas

- `types/` — tipos TS compartilhados (UUID, ISODate, etc.).
- `utils/` — funções puras (formatDate, clamp, etc.).
- `errors/` — classes de erro customizadas (`DomainError`, `ValidationError`).
